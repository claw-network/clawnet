# ==============================================================================
# ClawNet Chain — Server A (Primary: Validator + Bootstrap + API Gateway)
# ==============================================================================
# Usage:
#   cd /opt/clawnet
#   cp infra/chain-testnet/docker-compose.yml docker-compose.chain.yml
#   # Edit VALIDATOR_ADDRESS in .env
#   docker compose -f docker-compose.chain.yml up -d
#   docker compose -f docker-compose.chain.yml logs -f
# ==============================================================================
# Geth v1.13.15 — DO NOT upgrade to v1.14+ (Clique PoA removed)
# ==============================================================================

services:
  geth:
    image: ethereum/client-go:v1.13.15
    container_name: clawnet-geth
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: '2.0'
    ports:
      - "30303:30303/tcp"       # P2P TCP
      - "30303:30303/udp"       # P2P Discovery
      - "8545:8545/tcp"         # JSON-RPC (Caddy will reverse-proxy; UFW blocks direct access)
    volumes:
      - /opt/clawnet/chain-data:/data
      - /opt/clawnet/config:/config:ro
    entrypoint: >
      geth
        --datadir /data
        --networkid 7625
        --http
        --http.addr 0.0.0.0
        --http.port 8545
        --http.api eth,net,web3,txpool,clique,admin
        --http.corsdomain "*"
        --http.vhosts "*"
        --port 30303
        --mine
        --miner.etherbase ${VALIDATOR_ADDRESS}
        --unlock ${VALIDATOR_ADDRESS}
        --password /config/password.txt
        --allow-insecure-unlock
        --miner.gasprice 0
        --gpo.maxprice 0
        --gpo.ignoreprice 0
        --syncmode full
        --gcmode archive
        --bootnodes ""
    healthcheck:
      test: >
        wget -qO- --post-data='{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}'
        --header='Content-Type: application/json'
        http://127.0.0.1:8545 || exit 1
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
