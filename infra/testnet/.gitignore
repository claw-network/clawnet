# Real .env files contain secrets — never commit
.env
scenarios/.env
scenarios/init/*.env
scenarios/init/*.log
scenarios/init/RUN_REPORT.md
*.key
*.pem

# Runtime data (on production servers — NEVER wipe with git clean)
data/
chain-data/
config/
clawnetd-data/
node-data/
docker-compose.chain.yml
docker-compose.sync.yml
