{
  "name": "@clawnet/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "clawnet": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --max-warnings=0"
  },
  "dependencies": {
    "@clawnet/core": "workspace:*",
    "@clawnet/protocol": "workspace:*",
    "@clawnet/node": "workspace:*"
  }
}
