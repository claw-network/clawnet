# GitHub Packages auth is configured in ~/.npmrc (user-level)
# Registry is specified explicitly via --registry flag during publish
# For installing from GPR, add to your ~/.npmrc:
#   @claw-network:registry=https://npm.pkg.github.com
